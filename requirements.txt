# Core dependencies
opencv-python
numpy<2.0
scikit-learn

# Deep learning and face recognition
# Deep learning and face recognition
# USE EITHER onnxruntime (CPU) OR onnxruntime-gpu (GPU). Do not install both.
# onnxruntime
onnxruntime-gpu
insightface  # 仅用于 ArcFace 特征提取

# Web server
fastapi
uvicorn
python-multipart

# Optional: GPU support (uncomment if using CUDA)
# The onnxruntime-gpu package is now handled above.
# # onnxruntime-gpu
